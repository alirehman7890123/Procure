# .github/workflows/build-lubuntu.yml
name: Build Lubuntu Package

on:
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Linux system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1 \
            libegl1 \
            libxkbcommon-x11-0 \
            libdbus-1-3 \
            libfontconfig1 \
            libx11-xcb1 \
            libxcb-cursor0 \
            libxcb-icccm4 \
            libxcb-keysyms1 \
            libxcb-render-util0 \
            libxcb-xinerama0

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build app with PyInstaller
        run: |
          pyinstaller \
            --noconfirm \
            --clean \
            --onedir \
            --windowed \
            --name PharmacyPOS \
            --add-data "master_products.csv:." \
            --add-data "manufacturers.csv:." \
            starting.py

      - name: Create portable archive
        run: |
          tar -czf PharmacyPOS-lubuntu24.tar.gz -C dist PharmacyPOS

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PharmacyPOS-lubuntu24
          path: PharmacyPOS-lubuntu24.tar.gz
